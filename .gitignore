__pycache__/
*.pyc
.DS_Store
dist/

# Data and results are symlinked to off-repository storage; see README.
/data
/results

# Pixi-managed environment (pixi.lock is committed; the env itself is not).
.pixi/

# Snakemake's own run metadata.
.snakemake/
